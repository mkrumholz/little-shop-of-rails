<h2><%= @merchant.name %></h2>

<h3>My Items</h3>

<% @items.each do |item| %>
  <div id="item-<%= item.id %>">
    <p><a href="/merchants/<%= @merchant.id %>/items/<%= item.id %>" ><%= item.name %></a></p>
  </div>
<% end %>